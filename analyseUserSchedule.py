#analyseUserSchedule

def analyseUserSchedule():
    from accessUserSchedule import accessUserSchedule
    userSchedule = accessUserSchedule()
    if len(userSchedule) == 0:
        

analyseUserSchedule()
